# Fix Button Widths in Modals

## Steps
1. [x] Standardize confirm/cancel button styles in `ChangePassModal.tsx`
2. [x] Update `ConfirmCurrPassModal.tsx` to match `ChangePassModal.tsx` button styles exactly
3. [x] Verify both modals use identical width, padding, and margin values for buttons

